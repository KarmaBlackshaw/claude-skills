{
  "name": "agentic-ai",
  "version": "1.0.0",
  "private": true,
  "description": "Monorepo of Claude Code skills + Obsidian hub-and-spoke memory (jeash plugin).",
  "scripts": {
    "sync:plan-and-build": "bash sync.sh",
    "sync:plan-and-build:check": "bash sync.sh --check"
  }
}
